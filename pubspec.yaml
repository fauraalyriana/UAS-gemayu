name: flutter_app_uts
description: A new Flutter project.
publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=3.1.3 <4.0.0"

dependencies:
  cupertino_icons: ^1.0.2
  flutter:
    sdk: flutter
  image_picker: ^1.0.4
  intl: ^0.18.1

dev_dependencies:
  bloc: ^8.1.2
  dio: ^5.4.0
  equatable: ^2.0.5
  flutter_bloc: ^8.1.3
  flutter_launcher_icons: ^0.9.2
  flutter_lints: ^2.0.0
  flutter_test:
    sdk: flutter
  shared_preferences: ^2.2.2

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/GEMAYU.png"

flutter:
  assets:
    - assets/GEMAYU.png
    - assets/profile-bg3.jpg
    - assets/home.png
    - assets/body-bg.jpg
    - assets/bg2.png

  uses-material-design: true
